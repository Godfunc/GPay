spring:
  cloud:
    nacos:
      config:
        file-extension: yaml
        group: GPay
      discovery:
        group: GPay
  application:
    name: gpay-roll-channel-risk
  profiles:
    active: dev