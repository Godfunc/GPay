spring:
  cloud:
    nacos:
      config:
        file-extension: yaml
        group: GPay
      discovery:
        group: GPay
  application:
    name: gpay-delayed-notify
  profiles:
    active: dev